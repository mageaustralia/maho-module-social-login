name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    uses: mageaustralia/maho-ci/.github/workflows/module-checks.yml@main

  full:
    uses: mageaustralia/maho-ci/.github/workflows/full-checks.yml@main
